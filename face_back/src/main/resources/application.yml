spring:
  application:
    name: face_back
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/face?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456

mybatis:
  mapper-locations: classpath:mapper/*.xml # ????XML????
  type-aliases-package: org.evlast.face_back.domain # ??????

# ??MyBatis SQL??
logging:
  level:
    org:
      mybatis: DEBUG